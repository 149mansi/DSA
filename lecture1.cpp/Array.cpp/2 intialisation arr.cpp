#include <iostream>
using namespace std;
int main(){
    int arr[]={2,4,6,8,9};
    // int brr[5]={2,4,6,8,9};
// int char[5]={'a' 'b' 'c'....

    // but we can give array number 10 element  is more than the { } less element in brackt number then does give error because remainig space it will  write zeros
    // int crr[10]={2,4,6,8,9};

    // shows error because we give small size 4 is small and give array is 5 that is why give error
    // int arr[4]={2,4,6,8,9};

    cout<<"Array inatialisation succesfully"<<endl;
    return 0;
}
    
